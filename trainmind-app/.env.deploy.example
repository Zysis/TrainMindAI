# ============================================
# TrainMind — Variabili per il deploy su VPS
# Copia questo file in .env.deploy e compila i valori.
# NON committare .env.deploy su Git.
# ============================================

# ─── Domini (record A puntati all'IP del VPS) ─────────
# Dominio principale: alla radice il sito vetrina LAB21, sotto APP_BASE_PATH
# l'app TrainMind. È l'unica riga da cambiare se un domani il prodotto
# trasloca su un nome nuovo (poi va rifatta la build del container web,
# perché il sottopercorso finisce dentro il bundle di Next).
SITE_DOMAIN=tuodominio.com
# Sottopercorso dell'app. Cambiandolo vanno ricostruiti sia web sia lab21.
APP_BASE_PATH=/app

# Vecchio indirizzo dell'app: non serve più a servirla, ma tenerlo puntato
# al VPS fa funzionare i link di reset password già spediti e i segnalibri,
# che Caddy reindirizza con un 301 verso SITE_DOMAIN + APP_BASE_PATH.
APP_DOMAIN=app.tuodominio.com
API_DOMAIN=api.tuodominio.com
ATHLETE_DOMAIN=atleti.tuodominio.com
# Console di amministrazione (vista di piattaforma, sola lettura).
ADMIN_DOMAIN=admin.tuodominio.com

# Indirizzo a cui punta "Scopri di più" sul sito vetrina. Lasciare vuoto:
# il sito usa il percorso relativo /app dello stesso dominio. Si valorizza
# solo se TrainMind viene spostata su un dominio diverso da SITE_DOMAIN.
VITE_TRAINMIND_URL=

# ─── Database ─────────────────────────────────────────
POSTGRES_USER=trainmind
# Genera con: openssl rand -hex 24
POSTGRES_PASSWORD=CAMBIAMI_password_forte
POSTGRES_DB=trainmind_db

# ─── Auth ─────────────────────────────────────────────
# Metti a true per chiudere le registrazioni pubbliche (gli inviti atleti restano attivi)
DISABLE_REGISTRATION=false
# Genera con: openssl rand -hex 32
JWT_SECRET=CAMBIAMI_stringa_casuale_di_almeno_32_caratteri

# ─── OpenAI (obbligatoria per l'ai-service) ───────────
OPENAI_API_KEY=sk-...
# Modello di ripiego, usato solo se il routing per operazione qui sotto
# non specifica nulla.
OPENAI_MODEL=gpt-4o

# ─── Routing modelli AI ───────────────────────────────
# Quale modello usa ogni operazione. gpt-4o costa ~16 volte gpt-4o-mini:
# tenerlo su tutto significa pagare ~4,50 €/mese per cliente invece di ~0,76 €.
# Il modello completo resta solo dove la qualità paga: generazione dei piani
# (output lungo e strutturato) e Return-to-Play (ambito clinico).
# Per cambiare modello basta modificare qui e riavviare l'API.
AI_MODEL_CHAT=gpt-4o-mini
AI_MODEL_COACH=gpt-4o-mini
AI_MODEL_WELLNESS=gpt-4o-mini
AI_MODEL_REPORT=gpt-4o-mini
AI_MODEL_GENERATE=gpt-4o
AI_MODEL_RTP=gpt-4o

# Messaggi di storico chat inviati al modello a ogni turno.
# Ogni chiamata rimanda l'intera conversazione: senza limite il costo di una
# sessione lunga cresce in modo quadratico.
AI_CHAT_HISTORY_LIMIT=10

# In produzione non gira alcun server llama-cpp. Senza questo flag l'ai-service
# tenta un health check destinato a fallire prima di ogni chiamata a OpenAI,
# aggiungendo latenza a ogni richiesta.
LOCAL_LLM_ENABLED=false

# ─── Email (Resend) — reset password + inviti atleti + report ──────────
# Ottieni la API key gratis su https://resend.com/api-keys
# Senza chiave: le email vengono solo loggate (modalità "log-only").
# All'avvio l'API stampa una riga "Email: ..." che dichiara la modalità attiva.
RESEND_API_KEY=re_...

# Mittente dei report programmati.
REPORT_FROM_EMAIL=TrainMind <noreply@trainmind-app.com>

# Mittente delle email di autenticazione (reset password). Se vuota si ricade
# su REPORT_FROM_EMAIL. Il dominio deve essere verificato su Resend: con
# "onboarding@resend.dev" si possono raggiungere SOLO gli indirizzi del
# proprietario dell'account Resend, tutti gli altri ricevono 403.
AUTH_FROM_EMAIL=TrainMind <noreply@trainmind-app.com>

# ─── Stripe (opzionale, chiavi TEST per i test) ───────
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ─── Console di amministrazione ───────────────────────
#
# L'accesso NON e' un account TrainMind: e' l'utente e la password del
# basic_auth di Caddy, che vivono solo qui. Nessun utente della piattaforma,
# nemmeno un ADMIN di societa', puo' arrivare alla console.
#
# L'hash si genera sul server:
#   docker exec trainmind-caddy caddy hash-password
# Copiando il risultato QUI, raddoppia ogni $ in $$ — Compose interpreta il
# singolo $ come una variabile e la password non funzionerebbe piu'.
ADMIN_USER=admin
ADMIN_PASSWORD_HASH=$$2a$$14$$SOSTITUISCI_CON_L_HASH_GENERATO

# Connessione in SOLA LETTURA usata dalla console. L'utente si crea una volta
# con infra/sql/reporting-role.sql; la password e' quella scelta li' dentro.
# Nota: host `postgres`, cioe' il nome del servizio nella rete di Compose.
DATABASE_URL_READONLY=postgresql://trainmind_reporting:CAMBIAMI@postgres:5432/trainmind_db?schema=public

# Quali account NON contare come clienti veri (demo, prove interne).
# Sono pattern SQL LIKE separati da virgola, confrontati con l'email degli
# utenti: se un'organizzazione ha anche un solo utente che combacia, esce da
# tutte le statistiche.
ADMIN_DEMO_EMAIL_PATTERNS=%@demo.com,%@pro.com,%@starter.com,%@example.com
